// Актуальні продуктові константи. Посилання на рядки нижче вказують лише на
// архівне походження окремих значень із docs/prototype/nd-v2.dc.html.

/** Privacy-пункти онбордингу (vOb, рядки 1115–1120). */
export const OB_PRIVACY: { k: string; v: string }[] = [
  {k:'Які дані збираються',v:'Лише те, що ви вводите: симптоми, назви груп/станів, контекст, нотатки, дати циклу та дані поточного звіту.'},
  {k:'Де вони зберігаються',v:'Тільки у вашому браузері (localStorage). Це не захищене сховище реальних медичних даних.'},
  {k:'Чи залишають пристрій',v:'Ні. Поточна вебверсія не синхронізує записи й не передає їх у фоновому режимі.'},
  {k:'Хто має доступ і як видалити',v:'Кожен, хто має доступ до пристрою або профілю браузера, може прочитати локальні дані. Експорт CSV/JSON і повне видалення — у Налаштуваннях.'}
];

/** Чипи циклу в онбордингу (vOb, рядок 1122): [ключ, підпис]. */
export const OB_CYCLE_CHIPS: [string, string][] = [['on','Увімкнути'],['off','Не зараз']];

/** Заголовки кроків чек-іна (_vCheckin, рядок 1333); крок 3 — назва поточного симптому. */
export const CHECKIN_STEP_TITLES: Record<number, string> = {
  1:'Самопочуття',2:'Симптоми',4:'Контекст дня',5:'Нотатка й позначки',6:'Огляд і збереження'
};

/** Типи активності (_vCheckin actChips, рядок 1406). */
export const ACTIVITY_TYPES: string[] = ['Прогулянка','Вправи','Фізіотерапія','Інше'];

/** Додаткові контекстні позначки (_vCheckin extraChips, рядок 1409). */
export const CTX_EXTRA_CHIPS: string[] = ['Температура / лихоманка','Ознаки інфекції','Гаряча ванна / сауна','Стало краще після охолодження','Нові ліки / зміна дози','Пропуск прийому ліків','Падіння або травма','Задушливе приміщення'];

export type FlareFlagKey = 'isNew' | 'dur24' | 'temp';

/** Чипи загострення у кроці 5 чек-іна (_vCheckin flChips, рядок 1413). */
export const FLARE_CHECKIN_CHIPS: [FlareFlagKey, string][] = [
  ['isNew','Новий або значно сильніший за звичний'],
  ['dur24','Триває понад 24 години'],
  ['temp','Була температура / інфекція / перегрів']
];

/** Чипи загострення в діалозі позначки (_vDlg, рядок 1558). */
export const FLARE_DIALOG_CHIPS: [FlareFlagKey, string][] = [
  ['isNew','Новий або значно сильніший'],
  ['dur24','Триває понад 24 години'],
  ['temp','Температура / інфекція / перегрів']
];

/** Підписи фільтрів історії (_vHist FL, рядок 1454); порядок ключів = порядок чипів. */
export const HIST_FILTER_LABELS: Record<string, string> = {
  all:'Усі',flare:'Важливі зміни',menses:'Місячні',notes:'Нотатки',draft:'Чернетки'
};

/** Чипи типу нового власного симптому (_vMiscMain newTypeChips, рядок 1293). */
export const NEW_TYPE_CHIPS: [string, string][] = [['bool','Так / ні'],['scale','Шкала 1–5']];

/** Короткі підписи днів тижня для 7-денної смужки (vToday week7, рядок 1140). */
export const WD_SHORT: string[] = ['Пн','Вт','Ср','Чт','Пт','Сб','Нд'];
